// You can use the function from the `Math` module.

function sqrt(x){
 return Math.sqrt(x); 
}   
function power(x, y){
  return Math.pow(x, y); 
 }   
 function round(x){
  return Math.round(x); 
 }   
// Don't change the exported names. 
export {
  sqrt,
  power,
  round,
};
